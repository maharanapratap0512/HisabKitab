
# Hisab Kitab 2.0
HK 2

1.creat 'HK' folder.
2.open vscode in 'HK' folder
3.clone project by commnd - 'git clone https://github.com/maharanapratap0512/HisabKitab.git'

## GIT Commandas

git status
git add .
git status
git commit -am '-name/changes'
git push origin main

<!-- ## or ## -->

git pull origin main

Hints: 

1> --skip-tests lets us skip creating test files. It’s a boolean flag.

2> If getting error while making dist, we can find error path by using folling command:
ng build --prod --named-chunks --verbose --build-optimizer=false --source-map
or if --prod is unknown than
ng build --configuration production





